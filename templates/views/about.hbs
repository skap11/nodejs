<!DOCTYPE html>

<html>
    {{>head}}
    <body>
        <div class="main-content">
            {{>header}}
            <img src="/img/robot.png">
        </div>
        {{>footer}}
    </body>
</html>