<!DOCTYPE html>

<html>
    {{>head}}
    <body>
        <div class="main-content">
            {{>header}}
            <p>404, Page not found!</p>
        </div>
        {{>footer}}
    </body>
</html>